<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.basictv">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- TODO: Hardware Support ohne Touchscreen  -->
  

    <!-- TODO: Leanback feature  -->

   
    <!-- TODO: Fernbedienung handeln -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BasicTV">
        <!-- TODO: Banner Setzen -->
       
        <activity
            android:name=".MainActivity"
           
            android:exported="true"
            android:icon="@drawable/cat"
            android:label="@string/app_name"
            android:logo="@drawable/cat"
            android:screenOrientation="landscape">

            <!-- TODO: Leanback Launcher intent filter  -->
            


        </activity>
    </application>

</manifest>